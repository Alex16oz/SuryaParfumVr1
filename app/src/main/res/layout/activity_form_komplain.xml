<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <!-- Judul -->
        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Form Pengajuan Komplain"
            android:textSize="20sp"
            android:textStyle="bold"
            android:gravity="center"
            android:paddingBottom="16dp" />

        <!-- Nama -->
        <EditText
            android:id="@+id/etNama"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="Nama Lengkap"
            android:inputType="textPersonName" />

        <!-- No Order -->
        <EditText
            android:id="@+id/etNoOrder"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="Nomor Order"
            android:inputType="text"
            android:layout_marginTop="8dp" />

        <!-- Alasan -->
        <EditText
            android:id="@+id/etAlasan"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="Alasan Komplain"
            android:inputType="textMultiLine"
            android:minLines="4"
            android:gravity="top"
            android:layout_marginTop="8dp" />

        <!-- Preview Foto -->
        <ImageView
            android:id="@+id/imgBukti"
            android:layout_width="match_parent"
            android:layout_height="200dp"
            android:layout_marginTop="12dp"
            android:scaleType="centerCrop"
            android:background="#E0E0E0"
            android:contentDescription="Foto Bukti Komplain" />

        <!-- Tombol Pilih Foto -->
        <Button
            android:id="@+id/btnPilihFoto"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Pilih Foto"
            android:layout_marginTop="8dp" />

        <!-- Kirim -->
        <Button
            android:id="@+id/btnKirimKomplain"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Kirim Komplain"
            android:layout_marginTop="16dp" />

    </LinearLayout>
</ScrollView>
